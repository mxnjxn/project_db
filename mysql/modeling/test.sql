CREATE TABLE `test` (
  `seq` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(45) NULL,
  PRIMARY KEY (`seq`)
);

insert into test (
name 
) value (
"Tony"
);

drop tables test;

select * from test;

